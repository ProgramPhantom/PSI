import { ISpace } from "../space";

export const DEFAULT_SPACE: ISpace = {
	"padding": [0, 4, 0, 4],
	"offset": [0, 0],

	"contentWidth": 50,
	"contentHeight": 50,
	"placementMode": {"type": "free", "sizeMode": "fixed"},

	"ref": "space"
}