# Rect Element

The `Rect Element` is a simple rectangular object one can add to the canvas. You can change its fill colour, stroke colour and stroke thickness. This object is used for creating simple visual elemnets such as hard pulses.